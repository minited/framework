# TODO

- Smoke-test every demo page after changes (dark/light, reduced motion, keyboard focus).
- Consider a `.cluster--end` / `.cluster--start` modifier if needed.
- Consider hoisting `.btn--ghost` / `.btn--danger` into `core.css` if they become defaults.
